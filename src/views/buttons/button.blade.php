<div
    data-component="Button"
    data-style="{{ $type ?? 'default' }}"
    data-label="{{ $label }}"
    data-name="{{ $name ?? null }}"
    data-help="{{ $help ?? null }}"
    data-html-type="{{ $html_type ?? 'submit' }}"
    data-href="{{ $url ?? null }}"
></div>

<div
    data-component="NestedResource"
    data-url="{{ $url ?? '/' }}"
    data-icon="{{ $icon ?? 'plus'  }}"
    data-text="{{ $button ?? 'Create' }}"
    data-type="{{ $style ?? 'default' }}"
    data-size="{{ $size ?? 'default' }}"
>
</div>
